#!/usr/bin/env bash
#MISE description="Stop DynamoDB Local container"
set -euo pipefail

# shellcheck source=runtime.sh
source mise-tasks/db/runtime.sh

runtime="$(resolve_runtime)"
"$runtime" stop "$CONTAINER_NAME"
